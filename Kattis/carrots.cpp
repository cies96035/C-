#include<iostream>
using namespace std;

int n, p;
string str;
int main()
{
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    cin >> n >> p;
    while(cin >> str);
    cout << p << '\n';
    return 0;
}